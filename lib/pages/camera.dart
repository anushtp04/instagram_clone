import 'package:flutter/material.dart';

class UserCamera extends StatelessWidget {
  const UserCamera({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Camera"),
      ),
    );
  }
}
